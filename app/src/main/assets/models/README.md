Built-in model files are installed here by `scripts/download-deps.ps1`.

Do not commit placeholder `.param` or `.bin` files. Use domestic mirror URLs for
the Waifu2x and RealCUGAN archives, then run the script so these directories
match `app/src/main/assets/model_manifest.json`.
